import React, { Fragment } from "react";

const Header = () => (
  <Fragment>
    <header className="container">
      <h3>Bem vindo</h3>
      <h5 className="text-muted">
        App simples e leve para calcular a quantidade de deputados distritais.
      </h5>
    </header>
  </Fragment>
);
export default Header;
